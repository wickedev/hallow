{{#if generateComments}}
/**
 * Enum: {{name}}{{#if namespace}} (nested in {{namespace}}){{/if}}
 {{#if options.description}}
 * {{options.description}}
 {{/if}}
 * @generated by hallow-grpc-generator
 */
{{/if}}
export {{#if isConstEnum}}const {{/if}}enum {{enumName}} {
  {{#each values}}
  {{#if comment}}
  /** {{comment}} */
  {{/if}}
  {{name}} = {{number}},
  {{/each}}
}

{{#if generateHelpers}}
/**
 * Type guard to check if a value is a valid {{enumName}}
 * @param value - Value to check
 * @returns True if value is a valid {{enumName}}
 */
export function is{{enumName}}(value: any): value is {{enumName}} {
  return typeof value === 'number' && value in {{enumName}};
}

/**
 * Convert a number to {{enumName}}
 * @param value - Number value to convert
 * @returns {{enumName}} or undefined if invalid
 */
export function to{{enumName}}(value: number): {{enumName}} | undefined {
  return is{{enumName}}(value) ? value : undefined;
}

/**
 * Get the string name of a {{enumName}} value
 * @param value - {{enumName}} value
 * @returns String name of the enum value
 */
export function get{{enumName}}Name(value: {{enumName}}): string {
  return {{enumName}}[value];
}
{{/if}}
